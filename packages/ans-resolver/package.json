{
  "name": "@axioledger/ans-resolver",
  "version": "0.1.0",
  "description": "ANS Domain Resolver SDK — resolve .axq/.vpx/.sqx/.kpx/.vrq names on-chain via ANSRegistry contract",
  "main": "src/index.js",
  "types": "src/index.d.ts",
  "scripts": {
    "build":     "echo 'ANS Resolver: JS-only, no build step needed'",
    "typecheck": "echo 'ANS Resolver: no TypeScript yet — Phase 5 migration'",
    "test":      "jest --forceExit",
    "test:ci":   "jest --forceExit --ci --coverage",
    "clean":     "rm -rf dist coverage"
  },
  "license": "MIT",
  "dependencies": {
    "viem": "^2.17.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/src/__tests__/**/*.test.js"],
    "collectCoverageFrom": ["src/**/*.js", "!src/__tests__/**"],
    "coverageThreshold": {
      "global": { "lines": 60 }
    }
  }
}
