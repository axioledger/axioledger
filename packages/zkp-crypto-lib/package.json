{
  "name": "@axioledger/zkp-crypto-lib",
  "version": "0.1.0",
  "description": "ZK proof utilities — Groth16/PLONK proof generation, Wasm bindings for snarkjs",
  "main":   "dist/index.js",
  "types":  "dist/index.d.ts",
  "scripts": {
    "build":     "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test":      "jest --forceExit",
    "clean":     "rm -rf dist"
  },
  "license": "MIT",
  "dependencies": {
    "snarkjs": "^0.7.4"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "jest":       "^29.7.0"
  }
}
